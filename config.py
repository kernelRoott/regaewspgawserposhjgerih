import os
from dotenv import load_dotenv

load_dotenv()

BOT_TOKEN = os.getenv('BOT_TOKEN')
ADMIN_ID = int(os.getenv('ADMIN_ID', 0))
WEBAPP_URL = os.getenv('WEBAPP_URL', 'http://localhost:8080')
WEB_PORT = int(os.getenv('WEB_PORT', 8080))

# Настройки по умолчанию
DEFAULT_COOLDOWN = 30  # секунд
DEFAULT_STARS_REWARD = 15

# Тексты сообщений по умолчанию
MESSAGES = {
    'start': (
        'За каждую предложенную и одобренную публикацию Вы получите от {stars} Telegram звёзд.\n\n'
        '🎭 Если Вы считаете, что в публикации находитесь Вы/родственники в негативном ключе, '
        'то свяжитесь с Менеджером — @reportdirector\n'
        '❓Узнать статус публикации — /check #НомерПубликации'
    ),
    'submitted': '👌 Публикация успешно отправлена на проверку, ожидайте вердикта. Номер вашей публикации — #{number}',
    'approved': (
        '🎉 Ваша #{number} публикация прошла проверку!\n'
        'Администратор в скором времени отправит ваше вознаграждение.'
    ),
    'rejected': (
        '😔 К сожалению, публикация #{number} не прошла проверку.\n'
        'Не отчаивайтесь, возможно, у вас есть еще материалы, которые все хотят увидеть!'
    ),
    'cooldown': '⏳ Пожалуйста, подождите {seconds} секунд перед отправкой следующей публикации.'
}
